(executable
 (name {{ project_slug | snake_case }}_app)
 (libraries base stdio cmdliner lwt.unix {{ project_slug }}.lib)
 (flags -open Base)
 (modules (:standard)))

(install
 (section bin)
 (package {{ project_slug }})
 (files
  ({{ project_slug | snake_case }}_app.exe as {{ project_slug }})))

(include_subdirs unqualified)
